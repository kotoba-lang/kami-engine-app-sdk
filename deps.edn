{:paths ["src" "test"]
 ;; canvaskit: first-party pure-cljc viewport math (itself zero-dep) —
 ;; kami-ui-sdk.ui's LabelOverlay projection delegates to it (ADR-2607071130).
 :deps {
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       io.github.kotoba-lang/canvaskit
        {:git/url "https://github.com/kotoba-lang/canvaskit.git"
         :git/sha "fba5b558710faa1414f1e42573aa211645a864e1"}}
 :aliases {:test {:extra-paths ["test"]
                  :extra-deps {io.github.cognitect-labs/test-runner
                               {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}}
                  :main-opts ["-m" "cognitect.test-runner"]}
           ;; kami-ui-sdk.widgets (Slider/ColorSwatch/Carousel) is real DOM CLJS —
           ;; needed to compile/verify it in a browser. Nothing else in this repo
           ;; requires ClojureScript (see README).
           :cljs {:extra-paths ["dev"]
                  :extra-deps {org.clojure/clojurescript {:mvn/version "1.11.132"}}}
           ;; clj-kondo as a Clojars lib (no Homebrew/system install needed).
           :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
                  :main-opts ["-m" "clj-kondo.main" "--lint" "src" "test"
                              "--fail-level" "error"]}}}
